language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - gcc
      - g++
      - clang
      - cmake-3.15


before_script:
  - apt-get upgrade cmake
  - mkdir build
  - (cd build && cmake .. )

script:
  - (cd build && make)
  - (cd build && ctest --output-on-failure)