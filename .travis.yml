language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
      - gcc
      - g++
      - clang
      - cmake


before_script:
  - apt-get upgrade cmake
  - mkdir build
  - (cd build && cmake .. )

script:
  - (cd build && make)
  - (cd build && ctest --output-on-failure)